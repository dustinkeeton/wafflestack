# TRIAGE FRAGMENT — not a live manifest (nothing under triage/ is read by the
# render/validate pipeline). Verbatim pieces removed from bundles/expo-dev/bundle.yaml
# when mobile-architect was parked in round 1 of #38. To restore: `git mv` the agent
# back, re-add the entries below to that bundle.yaml, then re-run
# `npm run validate && node installer/cli.mjs render`.
#
# Removed manifest entries:
#   agents: [mobile-architect]
#
# Shared keys still declared in the live bundle (also used by remaining items):
#   project.name
#
# Removed config declarations (verbatim):
config:
  project.longName:
    required: true
    description: Full prose project descriptor, e.g. "the Synapse iOS/Android app".
# Per-item key usage (for reference when restoring):
#   agents/mobile-architect → project.longName, project.name (shared, still declared)
