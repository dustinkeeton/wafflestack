name: engineering-team
description: >-
  Product-engineering specialist roster — lead-engineer (the general
  architect / tech lead), data-engineer, qa-engineer, devops-engineer,
  ux-designer, and a remediating security-engineer, plus a browser-app
  webapp-security-audit skill. Agents are stack-agnostic skeletons; the stack,
  structure, pyramid, and briefs are project config. Pairs with the
  github-workflow stack (git-workflow/issue/github-project-management skills
  are referenced throughout) and slots into the orchestration stack's roster.
# Pre-selected by the setup wizard unless the user opts out (#202).
recommended: true
agents:
  [
    lead-engineer,
    data-engineer,
    qa-engineer,
    devops-engineer,
    ux-designer,
    security-engineer,
  ]
skills: [webapp-security-audit]
config:
  project.name:
    required: true
    description: Bare project name, e.g. "Plotto".
  project.longName:
    required: true
    description: Full prose project descriptor, e.g. "the Plotto lottery-analysis app".
  # #218 — the SINGLE-PROGRAM guard, on all four pre-flight commands. A project command reaches the
  # harness as a `Bash(<cmd>:*)` allowlist grant, which matches a command by its LEADING PROGRAM, so
  # a compound value (`tsc --noEmit && eslint .`) renders a grant that can match NOTHING: the call is
  # silently denied and the check never runs. The value is the consumer's, interpolated verbatim, so
  # the class is closed HERE — at the point of use — rather than by linting the templates downstream.
  # Byte-identical in code-quality / orchestration / github-workflow; guards union toolkit-wide.
  project.lintCmd:
    required: false
    default: npm run lint --if-present
    # BAN THE DELIMITERS, not the quotes. The value is spliced VERBATIM into the allowlist:
    #     --allowedTools 'Edit,Write,…,Bash(<cmd>:*),…'
    # The CLI parses that word TEXTUALLY — split on `,`, then read each entry as `Tool(spec)` —
    # BEFORE any shell or quote processing runs. So quoting cannot protect a delimiter: a `,` or a
    # `)` inside a "quoted" argument shatters the list exactly as a bare one does. The delimiters
    # are therefore banned UNCONDITIONALLY, with no quote-awareness anywhere in the pattern:
    #   `,`     separates the allowlist entries. `eslint --ext .js,.jsx,.ts,.tsx src` — the textbook
    #           multi-extension invocation, ONE program, no shell operator — shattered the list into
    #           `Bash(eslint --ext .js` plus junk `.jsx`/`.ts`/`.tsx src:*)` entries. The real command
    #           matched nothing and was SILENTLY DENIED: #218's exact failure mode, delivered through
    #           the mechanism built to eliminate it (#341 review).
    #   `(` `)` delimit the `Bash(…)` rule itself — a `)` closes it early. This also catches `<(…)`.
    #   `'`     terminates the single-quoted shell word (#254: "a single quote must never reach a
    #           shell literal"); `Bash(pytest -k 'not slow':*)` ends the word early and is mangled.
    #   CR/LF   break the `claude_args: >-` folded scalar.
#   LEADING/TRAILING WHITESPACE is banned too, for the same dead-grant reason: `npm test ` grants
#           `Bash(npm test :*)`, and the real `npm test` does not prefix-match a trailing space.
#           (YAML strips it from a plain scalar, so it takes an explicitly quoted value to hit —
#           but the grant it ships is just as dead, so the guard anchors both ends.)
    #   EMPTY   is banned by the `+`, and it is the degenerate case the `*` waved through (#351). An
    #           empty value renders `Bash(:*)` — an allowlist entry whose PREFIX IS THE EMPTY STRING,
    #           and every command starts with that. So the entry either grants ALL Bash or is inert,
    #           depending on how the CLI parses a degenerate rule; both are wrong, and a privilege
    #           boundary must not rest on which. A repo with no such check sets the shell no-op `true`
    #           (a narrow, harmless `Bash(true:*)` that exits 0) — never `""`. `true` must never be a
    #           shipped `default:`: a gate that passes VACUOUSLY is the silent false pass #218 is for.
    # …plus the #218 compound class itself (`;` `|` `&`, `$(…)`, backtick, `${{ }}`, a `cd ` prefix),
    # each of which renders a grant no real command can ever match.
    #
    # `:` is NOT banned: the rule's prefix marker is the LAST `:*`, so `npm run test:ci` grants
    # `Bash(npm run test:ci:*)` and matches fine. Banning it would false-reject a huge population.
    # `"` is NOT banned: it is inert at every landing site — literal inside the single-quoted shell
    # word, literal in the folded scalar, literal in a markdown code span, and a literal character in
    # the `Bash(…)` prefix, where it round-trips with the command the agent actually runs. The earlier
    # blanket quote ban rejected `go test -run "TestFoo" ./...` with no failure mode behind it.
    #
    # NO ALTERNATION, NO QUOTED SPANS. An earlier cut tried to permit a quoted `|`/`;` inside an
    # argument. Its fallback class also matched a quote, so the alternation RE-PAIRED the quotes — in
    # `eslint 'a' && prettier 'b'` the closing quote of the first argument and the opening quote of
    # the second were read as one span, swallowing the `&&`, and an ordinary lint command rendered a
    # DEAD GRANT. A flat negated character class cannot re-pair anything, so it stays flat.
    #
    # A guard for a silent-denial bug must fail CLOSED: reject the value, name the remedy, and let the
    # consumer wrap it in an npm script. A false reject is loud and trivially worked around; a false
    # accept is a dead grant nobody ever sees.
    pattern: '^(?!.*(?:\$\(|\$\{\{|`))(?!\s*cd\s)(?!\s)(?!.*\s$)[^;|&'',()\r\n]+$'
    patternHint: >-
      a project command must be ONE program the allowlist can grant as `Bash(<cmd>:*)`. It is
      spliced verbatim into `--allowedTools 'Edit,…,Bash(<cmd>:*),…'` — a list the CLI parses
      TEXTUALLY: it splits on `,` and reads each entry as `Tool(spec)`, before any shell or quote
      processing. So quoting cannot protect a delimiter — a `,` or a `)` inside an argument shatters
      the list exactly as a bare one does. Hence no `,`, no `(`/`)`, no `'`, no `&&` / `||` / `;` /
      `|` / `&`, no `cd ` prefix, no `$(…)`/backtick/`<(…)` substitution, and no newline: each
      renders a grant the real command cannot match, so the call is silently denied. Wrap it in an
      npm-script-style single entry point — put the compound (or the comma-separated argument, e.g.
      `eslint --ext .js,.ts .`) inside `package.json` or a Makefile and set this to the one command
      that runs it, e.g. `npm run ci` (#218). A `"` is fine: it is inert at every landing site.
      And it must not be EMPTY: an empty value grants `Bash(:*)`, and EVERY command has the empty
      string as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0 (#351).
    description: >-
      Lint/format check command referenced in agent pre-flight guidance. Must be ONE program the
      allowlist can grant as `Bash(<cmd>:*)` — no `,` (it separates the allowlist entries), no
      `(`/`)`, no `'`, no `&&` / `||` / `;` / `|`, no `cd ` prefix, no `$(…)`/backtick substitution,
      no newline. Wrap a compound — or a comma-separated argument like `eslint --ext .js,.ts .` — in
      an npm-script-style single entry point (`npm run ci`) instead.
      Never leave it EMPTY: an empty value grants `Bash(:*)`, and every command has the empty string
      as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0.
  project.typecheckCmd:
    required: false
    default: npx tsc --noEmit --skipLibCheck
    # BAN THE DELIMITERS, not the quotes. The value is spliced VERBATIM into the allowlist:
    #     --allowedTools 'Edit,Write,…,Bash(<cmd>:*),…'
    # The CLI parses that word TEXTUALLY — split on `,`, then read each entry as `Tool(spec)` —
    # BEFORE any shell or quote processing runs. So quoting cannot protect a delimiter: a `,` or a
    # `)` inside a "quoted" argument shatters the list exactly as a bare one does. The delimiters
    # are therefore banned UNCONDITIONALLY, with no quote-awareness anywhere in the pattern:
    #   `,`     separates the allowlist entries. `eslint --ext .js,.jsx,.ts,.tsx src` — the textbook
    #           multi-extension invocation, ONE program, no shell operator — shattered the list into
    #           `Bash(eslint --ext .js` plus junk `.jsx`/`.ts`/`.tsx src:*)` entries. The real command
    #           matched nothing and was SILENTLY DENIED: #218's exact failure mode, delivered through
    #           the mechanism built to eliminate it (#341 review).
    #   `(` `)` delimit the `Bash(…)` rule itself — a `)` closes it early. This also catches `<(…)`.
    #   `'`     terminates the single-quoted shell word (#254: "a single quote must never reach a
    #           shell literal"); `Bash(pytest -k 'not slow':*)` ends the word early and is mangled.
    #   CR/LF   break the `claude_args: >-` folded scalar.
#   LEADING/TRAILING WHITESPACE is banned too, for the same dead-grant reason: `npm test ` grants
#           `Bash(npm test :*)`, and the real `npm test` does not prefix-match a trailing space.
#           (YAML strips it from a plain scalar, so it takes an explicitly quoted value to hit —
#           but the grant it ships is just as dead, so the guard anchors both ends.)
    #   EMPTY   is banned by the `+`, and it is the degenerate case the `*` waved through (#351). An
    #           empty value renders `Bash(:*)` — an allowlist entry whose PREFIX IS THE EMPTY STRING,
    #           and every command starts with that. So the entry either grants ALL Bash or is inert,
    #           depending on how the CLI parses a degenerate rule; both are wrong, and a privilege
    #           boundary must not rest on which. A repo with no such check sets the shell no-op `true`
    #           (a narrow, harmless `Bash(true:*)` that exits 0) — never `""`. `true` must never be a
    #           shipped `default:`: a gate that passes VACUOUSLY is the silent false pass #218 is for.
    # …plus the #218 compound class itself (`;` `|` `&`, `$(…)`, backtick, `${{ }}`, a `cd ` prefix),
    # each of which renders a grant no real command can ever match.
    #
    # `:` is NOT banned: the rule's prefix marker is the LAST `:*`, so `npm run test:ci` grants
    # `Bash(npm run test:ci:*)` and matches fine. Banning it would false-reject a huge population.
    # `"` is NOT banned: it is inert at every landing site — literal inside the single-quoted shell
    # word, literal in the folded scalar, literal in a markdown code span, and a literal character in
    # the `Bash(…)` prefix, where it round-trips with the command the agent actually runs. The earlier
    # blanket quote ban rejected `go test -run "TestFoo" ./...` with no failure mode behind it.
    #
    # NO ALTERNATION, NO QUOTED SPANS. An earlier cut tried to permit a quoted `|`/`;` inside an
    # argument. Its fallback class also matched a quote, so the alternation RE-PAIRED the quotes — in
    # `eslint 'a' && prettier 'b'` the closing quote of the first argument and the opening quote of
    # the second were read as one span, swallowing the `&&`, and an ordinary lint command rendered a
    # DEAD GRANT. A flat negated character class cannot re-pair anything, so it stays flat.
    #
    # A guard for a silent-denial bug must fail CLOSED: reject the value, name the remedy, and let the
    # consumer wrap it in an npm script. A false reject is loud and trivially worked around; a false
    # accept is a dead grant nobody ever sees.
    pattern: '^(?!.*(?:\$\(|\$\{\{|`))(?!\s*cd\s)(?!\s)(?!.*\s$)[^;|&'',()\r\n]+$'
    patternHint: >-
      a project command must be ONE program the allowlist can grant as `Bash(<cmd>:*)`. It is
      spliced verbatim into `--allowedTools 'Edit,…,Bash(<cmd>:*),…'` — a list the CLI parses
      TEXTUALLY: it splits on `,` and reads each entry as `Tool(spec)`, before any shell or quote
      processing. So quoting cannot protect a delimiter — a `,` or a `)` inside an argument shatters
      the list exactly as a bare one does. Hence no `,`, no `(`/`)`, no `'`, no `&&` / `||` / `;` /
      `|` / `&`, no `cd ` prefix, no `$(…)`/backtick/`<(…)` substitution, and no newline: each
      renders a grant the real command cannot match, so the call is silently denied. Wrap it in an
      npm-script-style single entry point — put the compound (or the comma-separated argument, e.g.
      `eslint --ext .js,.ts .`) inside `package.json` or a Makefile and set this to the one command
      that runs it, e.g. `npm run ci` (#218). A `"` is fine: it is inert at every landing site.
      And it must not be EMPTY: an empty value grants `Bash(:*)`, and EVERY command has the empty
      string as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0 (#351).
    description: >-
      Type-check command referenced in agent pre-flight guidance. Must be ONE program the allowlist
      can grant as `Bash(<cmd>:*)` — no `,` (it separates the allowlist entries), no `(`/`)`, no
      `'`, no `&&` / `||` / `;` / `|`, no `cd ` prefix, no `$(…)`/backtick substitution, no newline.
      Wrap a compound — or a comma-separated argument like `eslint --ext .js,.ts .` — in an
      npm-script-style single entry point (`npm run ci`) instead.
      Never leave it EMPTY: an empty value grants `Bash(:*)`, and every command has the empty string
      as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0.
  project.testCmd:
    required: false
    default: npm test
    # BAN THE DELIMITERS, not the quotes. The value is spliced VERBATIM into the allowlist:
    #     --allowedTools 'Edit,Write,…,Bash(<cmd>:*),…'
    # The CLI parses that word TEXTUALLY — split on `,`, then read each entry as `Tool(spec)` —
    # BEFORE any shell or quote processing runs. So quoting cannot protect a delimiter: a `,` or a
    # `)` inside a "quoted" argument shatters the list exactly as a bare one does. The delimiters
    # are therefore banned UNCONDITIONALLY, with no quote-awareness anywhere in the pattern:
    #   `,`     separates the allowlist entries. `eslint --ext .js,.jsx,.ts,.tsx src` — the textbook
    #           multi-extension invocation, ONE program, no shell operator — shattered the list into
    #           `Bash(eslint --ext .js` plus junk `.jsx`/`.ts`/`.tsx src:*)` entries. The real command
    #           matched nothing and was SILENTLY DENIED: #218's exact failure mode, delivered through
    #           the mechanism built to eliminate it (#341 review).
    #   `(` `)` delimit the `Bash(…)` rule itself — a `)` closes it early. This also catches `<(…)`.
    #   `'`     terminates the single-quoted shell word (#254: "a single quote must never reach a
    #           shell literal"); `Bash(pytest -k 'not slow':*)` ends the word early and is mangled.
    #   CR/LF   break the `claude_args: >-` folded scalar.
#   LEADING/TRAILING WHITESPACE is banned too, for the same dead-grant reason: `npm test ` grants
#           `Bash(npm test :*)`, and the real `npm test` does not prefix-match a trailing space.
#           (YAML strips it from a plain scalar, so it takes an explicitly quoted value to hit —
#           but the grant it ships is just as dead, so the guard anchors both ends.)
    #   EMPTY   is banned by the `+`, and it is the degenerate case the `*` waved through (#351). An
    #           empty value renders `Bash(:*)` — an allowlist entry whose PREFIX IS THE EMPTY STRING,
    #           and every command starts with that. So the entry either grants ALL Bash or is inert,
    #           depending on how the CLI parses a degenerate rule; both are wrong, and a privilege
    #           boundary must not rest on which. A repo with no such check sets the shell no-op `true`
    #           (a narrow, harmless `Bash(true:*)` that exits 0) — never `""`. `true` must never be a
    #           shipped `default:`: a gate that passes VACUOUSLY is the silent false pass #218 is for.
    # …plus the #218 compound class itself (`;` `|` `&`, `$(…)`, backtick, `${{ }}`, a `cd ` prefix),
    # each of which renders a grant no real command can ever match.
    #
    # `:` is NOT banned: the rule's prefix marker is the LAST `:*`, so `npm run test:ci` grants
    # `Bash(npm run test:ci:*)` and matches fine. Banning it would false-reject a huge population.
    # `"` is NOT banned: it is inert at every landing site — literal inside the single-quoted shell
    # word, literal in the folded scalar, literal in a markdown code span, and a literal character in
    # the `Bash(…)` prefix, where it round-trips with the command the agent actually runs. The earlier
    # blanket quote ban rejected `go test -run "TestFoo" ./...` with no failure mode behind it.
    #
    # NO ALTERNATION, NO QUOTED SPANS. An earlier cut tried to permit a quoted `|`/`;` inside an
    # argument. Its fallback class also matched a quote, so the alternation RE-PAIRED the quotes — in
    # `eslint 'a' && prettier 'b'` the closing quote of the first argument and the opening quote of
    # the second were read as one span, swallowing the `&&`, and an ordinary lint command rendered a
    # DEAD GRANT. A flat negated character class cannot re-pair anything, so it stays flat.
    #
    # A guard for a silent-denial bug must fail CLOSED: reject the value, name the remedy, and let the
    # consumer wrap it in an npm script. A false reject is loud and trivially worked around; a false
    # accept is a dead grant nobody ever sees.
    pattern: '^(?!.*(?:\$\(|\$\{\{|`))(?!\s*cd\s)(?!\s)(?!.*\s$)[^;|&'',()\r\n]+$'
    patternHint: >-
      a project command must be ONE program the allowlist can grant as `Bash(<cmd>:*)`. It is
      spliced verbatim into `--allowedTools 'Edit,…,Bash(<cmd>:*),…'` — a list the CLI parses
      TEXTUALLY: it splits on `,` and reads each entry as `Tool(spec)`, before any shell or quote
      processing. So quoting cannot protect a delimiter — a `,` or a `)` inside an argument shatters
      the list exactly as a bare one does. Hence no `,`, no `(`/`)`, no `'`, no `&&` / `||` / `;` /
      `|` / `&`, no `cd ` prefix, no `$(…)`/backtick/`<(…)` substitution, and no newline: each
      renders a grant the real command cannot match, so the call is silently denied. Wrap it in an
      npm-script-style single entry point — put the compound (or the comma-separated argument, e.g.
      `eslint --ext .js,.ts .`) inside `package.json` or a Makefile and set this to the one command
      that runs it, e.g. `npm run ci` (#218). A `"` is fine: it is inert at every landing site.
      And it must not be EMPTY: an empty value grants `Bash(:*)`, and EVERY command has the empty
      string as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0 (#351).
    description: >-
      Test command the qa-engineer runs and reports on. Must be ONE program the allowlist can grant
      as `Bash(<cmd>:*)` — no `,` (it separates the allowlist entries), no `(`/`)`, no `'`, no `&&`
      / `||` / `;` / `|`, no `cd ` prefix, no `$(…)`/backtick substitution, no newline. Wrap a
      compound — or a comma-separated argument like `eslint --ext .js,.ts .` — in an
      npm-script-style single entry point (`npm run ci`) instead.
      Never leave it EMPTY: an empty value grants `Bash(:*)`, and every command has the empty string
      as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0.
  project.buildCmd:
    required: false
    default: npm run build
    # BAN THE DELIMITERS, not the quotes. The value is spliced VERBATIM into the allowlist:
    #     --allowedTools 'Edit,Write,…,Bash(<cmd>:*),…'
    # The CLI parses that word TEXTUALLY — split on `,`, then read each entry as `Tool(spec)` —
    # BEFORE any shell or quote processing runs. So quoting cannot protect a delimiter: a `,` or a
    # `)` inside a "quoted" argument shatters the list exactly as a bare one does. The delimiters
    # are therefore banned UNCONDITIONALLY, with no quote-awareness anywhere in the pattern:
    #   `,`     separates the allowlist entries. `eslint --ext .js,.jsx,.ts,.tsx src` — the textbook
    #           multi-extension invocation, ONE program, no shell operator — shattered the list into
    #           `Bash(eslint --ext .js` plus junk `.jsx`/`.ts`/`.tsx src:*)` entries. The real command
    #           matched nothing and was SILENTLY DENIED: #218's exact failure mode, delivered through
    #           the mechanism built to eliminate it (#341 review).
    #   `(` `)` delimit the `Bash(…)` rule itself — a `)` closes it early. This also catches `<(…)`.
    #   `'`     terminates the single-quoted shell word (#254: "a single quote must never reach a
    #           shell literal"); `Bash(pytest -k 'not slow':*)` ends the word early and is mangled.
    #   CR/LF   break the `claude_args: >-` folded scalar.
#   LEADING/TRAILING WHITESPACE is banned too, for the same dead-grant reason: `npm test ` grants
#           `Bash(npm test :*)`, and the real `npm test` does not prefix-match a trailing space.
#           (YAML strips it from a plain scalar, so it takes an explicitly quoted value to hit —
#           but the grant it ships is just as dead, so the guard anchors both ends.)
    #   EMPTY   is banned by the `+`, and it is the degenerate case the `*` waved through (#351). An
    #           empty value renders `Bash(:*)` — an allowlist entry whose PREFIX IS THE EMPTY STRING,
    #           and every command starts with that. So the entry either grants ALL Bash or is inert,
    #           depending on how the CLI parses a degenerate rule; both are wrong, and a privilege
    #           boundary must not rest on which. A repo with no such check sets the shell no-op `true`
    #           (a narrow, harmless `Bash(true:*)` that exits 0) — never `""`. `true` must never be a
    #           shipped `default:`: a gate that passes VACUOUSLY is the silent false pass #218 is for.
    # …plus the #218 compound class itself (`;` `|` `&`, `$(…)`, backtick, `${{ }}`, a `cd ` prefix),
    # each of which renders a grant no real command can ever match.
    #
    # `:` is NOT banned: the rule's prefix marker is the LAST `:*`, so `npm run test:ci` grants
    # `Bash(npm run test:ci:*)` and matches fine. Banning it would false-reject a huge population.
    # `"` is NOT banned: it is inert at every landing site — literal inside the single-quoted shell
    # word, literal in the folded scalar, literal in a markdown code span, and a literal character in
    # the `Bash(…)` prefix, where it round-trips with the command the agent actually runs. The earlier
    # blanket quote ban rejected `go test -run "TestFoo" ./...` with no failure mode behind it.
    #
    # NO ALTERNATION, NO QUOTED SPANS. An earlier cut tried to permit a quoted `|`/`;` inside an
    # argument. Its fallback class also matched a quote, so the alternation RE-PAIRED the quotes — in
    # `eslint 'a' && prettier 'b'` the closing quote of the first argument and the opening quote of
    # the second were read as one span, swallowing the `&&`, and an ordinary lint command rendered a
    # DEAD GRANT. A flat negated character class cannot re-pair anything, so it stays flat.
    #
    # A guard for a silent-denial bug must fail CLOSED: reject the value, name the remedy, and let the
    # consumer wrap it in an npm script. A false reject is loud and trivially worked around; a false
    # accept is a dead grant nobody ever sees.
    pattern: '^(?!.*(?:\$\(|\$\{\{|`))(?!\s*cd\s)(?!\s)(?!.*\s$)[^;|&'',()\r\n]+$'
    patternHint: >-
      a project command must be ONE program the allowlist can grant as `Bash(<cmd>:*)`. It is
      spliced verbatim into `--allowedTools 'Edit,…,Bash(<cmd>:*),…'` — a list the CLI parses
      TEXTUALLY: it splits on `,` and reads each entry as `Tool(spec)`, before any shell or quote
      processing. So quoting cannot protect a delimiter — a `,` or a `)` inside an argument shatters
      the list exactly as a bare one does. Hence no `,`, no `(`/`)`, no `'`, no `&&` / `||` / `;` /
      `|` / `&`, no `cd ` prefix, no `$(…)`/backtick/`<(…)` substitution, and no newline: each
      renders a grant the real command cannot match, so the call is silently denied. Wrap it in an
      npm-script-style single entry point — put the compound (or the comma-separated argument, e.g.
      `eslint --ext .js,.ts .`) inside `package.json` or a Makefile and set this to the one command
      that runs it, e.g. `npm run ci` (#218). A `"` is fine: it is inert at every landing site.
      And it must not be EMPTY: an empty value grants `Bash(:*)`, and EVERY command has the empty
      string as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0 (#351).
    description: >-
      Production build command (pre-flight guidance and bundle-output secret scan). Must be ONE
      program the allowlist can grant as `Bash(<cmd>:*)` — no `,` (it separates the allowlist
      entries), no `(`/`)`, no `'`, no `&&` / `||` / `;` / `|`, no `cd ` prefix, no `$(…)`/backtick
      substitution, no newline. Wrap a compound — or a comma-separated argument like `eslint --ext
      .js,.ts .` — in an npm-script-style single entry point (`npm run ci`) instead.
      Never leave it EMPTY: an empty value grants `Bash(:*)`, and every command has the empty string
      as a prefix. If this project has no such check, set the shell no-op `true` — it grants a
      harmless `Bash(true:*)` and exits 0.
  project.lockfile:
    required: false
    default: package-lock.json
    description: Committed lockfile name (e.g. pnpm-lock.yaml) checked against package.json drift.
  planner.productDocsDir:
    required: false
    default: docs/product/
    description: Directory where user stories / product briefs live (read by qa-engineer).
  lead.adrDir:
    required: false
    default: docs/adr/
    description: Directory where the lead engineer files architecture decision records (ADRs).
  lead.stack:
    required: true
    description: Verbatim body of the lead-engineer "Stack (locked)" section — the project's approved technology list.
  lead.structure:
    required: true
    description: Verbatim body of the lead-engineer "Structure" section — module layout and boundary rules.
  lead.delegation:
    required: true
    description: >-
      Verbatim sentence(s) completing the lead-engineer "Delegate." principle —
      which specialist gets which kind of work, e.g. "UX work to `ux-designer`,
      tests to `qa-engineer`, …. Use the `Agent` tool to invoke them."
  data.brief:
    required: true
    description: >-
      Verbatim block dropped into the data-engineer agent: the project's data
      source(s) (endpoints, shapes, quirks), data-module layout, and guidance for
      the analytical insights the project derives. Markdown sections welcome.
  qa.pyramid:
    required: true
    description: Verbatim body of the qa-engineer "Test pyramid" section — the project's test tiers with tooling and mocking boundaries.
  qa.fixtures:
    required: true
    description: Verbatim body of the qa-engineer "Fixtures" section — where fixtures live and how they are captured.
  devops.ci:
    required: true
    description: Verbatim body of the devops-engineer "CI baseline" section — the ordered pipeline steps.
  devops.dependencies:
    required: true
    description: Verbatim body of the devops-engineer "Dependencies" section — package manager, update automation, and major-version policy.
  ux.guidelines:
    required: true
    description: >-
      Verbatim block dropped into the ux-designer agent: the project's design
      system (palette, typography, density, motion) and any chart/component
      standards. Markdown sections welcome.
  security.agentGitignore:
    required: true
    description: Plain (non-code-formatted) list of sensitive paths for the security-engineer's gitignore check, e.g. ".env, data.json, .synapse/, node_modules/".
  security.threatModel:
    required: true
    description: Verbatim bullet list of the project's real attack surface (external APIs, external processes, file access, secret storage).
  sec.threatModelIntro:
    required: true
    description: >-
      Verbatim intro paragraph of the webapp-security-audit skill describing the
      app's actual threat surface (what exists, what doesn't — server, auth, PII).
  sec.dataSource:
    required: true
    description: The untrusted external data source named in validation guidance, e.g. "calottery.com".
  sec.auditCmd:
    required: false
    default: npm audit --omit=dev
    description: Production-dependency audit command, e.g. "pnpm audit --prod".
